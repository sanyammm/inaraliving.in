// Placeholder for form validation middleware
const validationMiddleware = (req, res, next) => {
  // Validate req.body
  next();
};

export default validationMiddleware; // Use ES module default export
