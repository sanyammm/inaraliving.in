// Utility to send WhatsApp message (placeholder)
const sendWhatsApp = async (phone, message) => {
  console.log(`Sending WhatsApp to ${phone}`);
};

export default sendWhatsApp; // Use ES module default export
